# Baseline evidence placeholder

The first remote M0 preflight will create a run-specific manifest under this
directory after the user confirms the sole SSH host/alias. No host is inferred
from SSH configuration, and no baseline result is claimed before the commands
have run on that target.
